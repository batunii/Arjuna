Major changes : 
1. [DONE] ColorPop is just a shader now, that is not being used. The filter that is being used is SignPop and the testing is done on that as well, all the mentions of ColorPop needs to be examined and tested whether they are mentioning the tested filters or just referring to the filter.
   Done: audited every ColorPop mention across ch1, ch3-ch8 in the .tex source. Fixed naming/framing everywhere ColorPop was wrongly described as the tested/evaluated mode instead of SignPop (its detector-gated descendant). ColorPop mentions that correctly refer to it as the underlying pipeline (not the tested mode) were left as-is.
2. [DONE] Filter names — rename to what they actually are, since names like "ColorPop" carry stylistic-rendering connotations that could mislead an examiner.
   Done: ch4 mode headings renamed to mechanism-first-then-code-name convention (e.g. "Hard Dark", "Soft Dark", "SignPop" each introduced by what they do before the code name sticks). Reply to John's email also addressed this directly.
3. [DONE] Differentiate the world-locked "focus window" (user-placed, static) from the detector-triggered "detection aperture" (transient, opens on confirmed detection) — consistently, throughout.
   Done: added an explicit definition block distinguishing the two at the start of ch4 §4.2. Replaced every loose "a locally opened window" phrasing in ch1/ch6 with "a detection aperture opened over the detected object" so the term is used consistently once defined.

Chapter wise changes : 
Chapter 1 : 
1. [DONE] The 1.3 sections of the report talks about the second choice but gets little confused... [VERIFY - instead of blindly following the instrcution, check whether this makes sense or this will hurt the dissertation]
   Verified and applied: the corrected framing is that the study decouples the human-factors question from *live* detection specifically, not from detection itself (SignPop's colour-pop gate is always detection-gated; only the live vs. offline-oracle distinction is what's scoped out). Rewrote the §1.3 paragraph accordingly and fixed RQ2/C2 to name SignPop. The "removed for Hard Dark testing, documented as a scope limitation" framing checked out and was kept — it's honest rather than evasive, so it does not hurt the dissertation.

2. [DONE] The pending result problem — testing is ongoing but the dissertation should report the N=11 data already collected as findings, not as pending.
   Done 2026-08-06, using `Only-the-Eye-of-the-Bird-TIGHT.pptx` as the authoritative source (saved to memory as `dissertation_interim_results` — check it before touching any results prose again). Ch6 now has a new §6.11 "Interim Results (N=11)" reporting the real numbers: Block A (Hard Dark, real hardware) +3.65pp accuracy (p=.036, dz=0.73, 9/11 improved); Block B (SignPop) pooled safety check +3.9pp/not lost (worst case ~5pp loss, within the 10pp margin); the real headline finding is a +18.6pp detection gain specific to the 20-30° eccentricity band (p=.028, replicates across both collection phases), not an overall speedup. Along the way, discovered and fixed two things ch6 got wrong about its own methods: Block A's "2x2 distractor-present/absent" design was never actually run (only Filter vs NoFilter, distractors always on — exactly the correction from earlier in the session about A1-A4 not being real conditions), and Block B's "24 scripted diamond-flash probes, central/peripheral bands" design was never built either — the real task is clicking on real traffic-light/pedestrian targets in the driving footage, scored by eccentricity band via the marker's reconstructed on-screen position. H2a/H2b redefined accordingly (H2a = 20-30° band gain, H2b = pooled safety). Also fixed a stale ch6 paragraph wrongly claiming Block A's browser-prototype-to-VR-harness port was still outstanding — it isn't; Block A ran on real hardware with a room-anchored window, per the pptx. Propagated the interim numbers into ch1 (§1.5 status, roadmap, C4), ch5 (status framing, benchmarks vs. user-study timeline decoupled), ch7 (new §7.2 "Interim results, discussed", subjective/objective divergence, updated Sensitivity-floor limitation), and ch8 (question-revisited, C3/C4, removed the literal "[Update once data exist]" placeholder, added the three-arm follow-up as a 7th future-work item).
   Important: don't re-derive Block A/B statistics from raw CSVs from scratch again — see the memory file for why a naive pooled re-aggregation gives a wrong, much less favourable picture than the real finding.

9. [DONE] Full sweep across all chapters against the pptx (`Only-the-Eye-of-the-Bird-TIGHT.pptx`), 2026-08-06.
   Verified "ten modes" is correct against the actual code enum (CameraSphereVignetteManager.cs) — the pptx's "nine filters" is a narrative simplification (folds ColorPop into "SignPop's substrate"), not an error to copy in. Fixed ch6's mode-rejection rationale, which was missing 2 of the 8 excluded modes (Blur and ColorPop) and had no per-mode reasons for the pilot-lost group — added Blur's literature rejection (59/102 disliked it) and the specific pilot-observed failure for each of ConspicuitySqueeze (brightened instead of squeezing), OutlinedDark (too visually interesting), GranulatedPeriphery (occludes detail), and SpotLift (can't be built on passthrough at all). Fixed ch1's RQ1/RQ2/H1/H2a/H2b summary, which still described the old 2x2 distractor-interaction design and the old central/peripheral probe split. Fixed the frontmatter.tex abstract and Summary of Contributions, which said nothing about the interim N=11 results at all.
   Also went back through everything written in this session's earlier interim-results pass and reworked it to actually follow the `dissertation-writing` skill (plain sentences, no em-dashes/semicolons joining two ideas) — it had been skipped the first time through.

3. [DONE] Chapter 2 restructure into a six-step narrative (distraction problem real → prior guidance techniques → has removal/DR been tried and did it work → can it be built on current hardware → what to test for → how to test it), rather than a topic-by-topic puzzle.
   Checked the proposed order against ch2's actual argument-dependency structure (no section presupposes content from a later one under the new order) before applying it — it holds. Final order: 2.1 Foundations (unchanged) -> 2.2 Saliency/Guidance (was 2.3) -> 2.3 DR/VNC (was 2.2) -> 2.4 Comfort (unchanged) -> 2.5 Platform (was 2.6) -> 2.6 Applied Contexts (was 2.5) -> 2.7 Evaluation (unchanged) -> 2.8 Synthesis (unchanged). All 9 internal cross-references (11 number substitutions) updated; References section untouched; opening roadmap paragraph rewritten to match, fixing a pre-existing "five bodies of literature" typo (should be eight) along the way.

4. [DONE] Housekeeping note — checked ch1 and ch2 .tex source; no such note exists in either. Already resolved (removed before this session, or never present in the .tex source — only the stale .md working copy may have had one, and .md is no longer the source of truth).

5. [DONE] Same item as #3 above — chapter 2 now reads as one continuous argument rather than eight independent literature sections.

6. [DONE] The inherent tension [2.1.3, now unchanged under the reorg] — added to justify both legs of the experiment. [VERIFY - if we should add it here or not]
   Checked against the chapter's content: H1's grounding (extraneous-load reduction) is already established one subsection earlier, so naming H1 alongside the existing H2b here is a natural fit, not a stretch. Added one sentence naming the benefit hypothesis (H1) ahead of the existing risk-hypothesis (H2b) sentence, mirroring the tension being described.

7. [PARTIAL] Using filter names without explaining what they are/what to imagine might be problematic.
   Done for ch4 (mode headings renamed mechanism-first) and ch2 (SignPop now glossed at its true first mention — previously not glossed anywhere in the chapter). Not yet swept across ch1/ch3/ch5-ch8 for every remaining bare mode-name mention — flagging as still open if a full pass is wanted.

8. [DONE, superseding the earlier narrower pass] Soft Dark must never be discussed as a separate thing anywhere in the dissertation, only ever grouped with other filters.
   Earlier resolution (kept ch2's design-rationale sentence as-is, reasoning it was a citation not a testing claim) was too narrow. On a stricter re-read, removed every standalone/dedicated treatment: ch4's own `\subsection{Partial peripheral attenuation (Soft Dark)}` heading (merged into the Hard Dark subsection, now `Peripheral occlusion (Hard Dark and Soft Dark)`, since both are literally the same shader path at different opacity ceilings); ch3 §3.5's ~14-line "Soft Dark's position deserves explicit defence... four grounds" paragraph (deleted outright — its substance was already covered by the fidelity/control, suppression/awareness, and deployability axis paragraphs immediately above it, which discuss Soft Dark grouped with Hard Dark and SignPop); ch6 §6.4.2's dedicated "Soft Dark was not tested for the same reason Hard Dark was tested..." paragraph (folded down to one clause inside the shared four-reasons paragraph). Also fixed two places (ch5's T5 benchmark claim, ch8's "Hour-long deployment" future-work item) that wrongly called Soft Dark "the only/one mode with no camera pipeline" when Hard Dark is equally camera-free — broadened both to "the dark modes." Also broadened one ch2 sentence ("Soft Dark's partial-opacity ceiling" → "the dark modes' graduated opacity ceilings") since it singled Soft Dark out of a three-item list where Hard Dark equally applied. Remaining Soft Dark mentions (ch1 figure caption, ch3 axis/table mentions, ch5 benchmark config table row, ch6 RQ3/Block C task description, ch6 threats-table row, ch7 limitations-list item) were individually checked and left as-is: each names Soft Dark alongside Hard Dark/SignPop in the same sentence, or is a compact, unavoidable factual reference (a table row, a one-line limitation), not exposition about Soft Dark as a topic in its own right. Also did the same residue sweep the user asked for on ch7/ch8 (already clean, confirmed via targeted grep) and re-confirmed ch1–ch5 have no leftover 2×2/MC1/DRT-probe language (grep clean, only bibliography-URL false positives). Brace-balance re-verified on all 8 chapters after every edit (ch1 70/70, ch2 285/285, ch3 143/143, ch4 164/164, ch5 142/142, ch6 366/366, ch7 91/91, ch8 29/29).

