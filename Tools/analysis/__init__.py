"""Analysis package for the DR attention-guidance user study.

Pipeline (see Tools/README-study-tools.md):
    parse -> h1_lmm / h2_tost / likert -> report
Each module is importable and has a CLI (`python -m` from Tools/, or direct path).
"""
